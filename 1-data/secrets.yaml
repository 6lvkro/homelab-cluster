apiVersion: v1
kind: Secret
metadata:
  name: postgres
  namespace: default
type: Opaque
stringData:
  DB_PASSWORD_POSTGRES: "${DB_PASSWORD_POSTGRES}"
---
apiVersion: v1
kind: Secret
metadata:
  name: mariadb
  namespace: default
type: Opaque
stringData:
  DB_PASSWORD_MARIADB: "${DB_PASSWORD_MARIADB}"
